export enum Role {
  USER = "USER",
  ADMIN = "ADMIN",
}

export enum BookingStatus {
  PENDING = "PENDING",
  CONFIRMED = "CONFIRMED",
  CANCELED = "CANCELED",
  RESCHEDULED = "RESCHEDULED",
  COMPLETED = "COMPLETED",
}
